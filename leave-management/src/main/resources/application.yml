spring:
  datasource:
    url: jdbc:postgresql://aws-1-ap-southeast-1.pooler.supabase.com:5432/postgres?user=postgres.kwpkidgxsuyzgbkpbqlq&password=${DB_PASSWORD}
    username: postgres.kwpkidgxsuyzgbkpbqlq
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  flyway:
    enabled: false

server:
  port: 8080